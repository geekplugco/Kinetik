Mono uppercase breadcrumb trail for collection and product pages. The last item is the current page (not clickable).

```jsx
<Breadcrumbs items={['Home', 'Outerwear', 'Shell-01 Hardshell']} onNavigate={(c, i) => go(c)} />
```
