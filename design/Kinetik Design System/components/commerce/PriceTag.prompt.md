Mono price block. Pass `compareAt` higher than `price` to show a struck original.

```jsx
<PriceTag price={420} />
<PriceTag price={336} compareAt={420} size="lg" />
```

The sale price stays ink — volt is reserved for the Tag/CTA, not the number. Use `size="lg"` on PDP.
