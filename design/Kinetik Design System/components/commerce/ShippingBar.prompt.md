Free-shipping progress bar for the top of the cart drawer. Volt fill that flips to an "unlocked" state at the threshold.

```jsx
<ShippingBar current={subtotal} threshold={200} />
```

Place it directly under the drawer header, above the line items.
