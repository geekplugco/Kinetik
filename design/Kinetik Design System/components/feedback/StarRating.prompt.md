Read-only star rating with fractional fill. Stars stay ink — volt is reserved for conversion.

```jsx
<StarRating value={4.5} count={128} showValue />
<StarRating value={3} size={13} />
```

Use on product cards (compact) and PDP (with count). For a writable review form, drive the value yourself and overlay buttons.
