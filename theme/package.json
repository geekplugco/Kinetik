{
  "name": "kinetik-theme",
  "private": true,
  "description": "Kinetik Shopify theme — no-build (Tailwind v4 browser CDN in dev, committed minified CSS in prod)",
  "scripts": {
    "smoke": "node scripts/smoke-theme.mjs",
    "dev": "shopify theme dev",
    "push": "shopify theme push"
  }
}
